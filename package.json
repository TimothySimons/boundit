{
    "name": "boundit",
    "version": "1.0.0",
    "devDependencies": {
        "esbuild": "^0.21.4",
        "lite-server": "^2.6.1",
        "typescript": "^5.4.5"
    },
    "scripts": {
        "dist": "esbuild src/tutorialLoadImage.ts --bundle --format=esm --outfile=dist/bundle.js",
        "start": "lite-server --config=local-server.json"
    }
}
